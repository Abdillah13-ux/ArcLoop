{
  "name": "@arcloop/db",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo \"db lint not configured yet\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"db tests not configured yet\"",
    "format": "echo \"db format not configured yet\""
  },
  "dependencies": {
    "drizzle-orm": "^0.44.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.4",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.8.3"
  }
}
